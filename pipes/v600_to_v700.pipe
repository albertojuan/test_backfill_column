NODE live_to_new
DESCRIPTION >
    live to new

SQL >
    SELECT
        timestamp, session_id, action, version, payload
    FROM v6_0_0.analytics_events

TYPE materialized
DATASOURCE analytics_events